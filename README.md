# sutton-home

Independent Demo landing for https://sutton.top (benji.org-style list).

AI Portal lives separately at https://portal.sutton.top (Cloudflare Pages project `sutton-top`).

## Deploy

```bash
npx wrangler pages deploy . --project-name=sutton-home --commit-dirty=true
```
